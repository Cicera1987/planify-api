version: '3'
services:
  app:
    build: .
    ports:
      - '3001:3000'
    environment:
      - MONGODB_URI=mongodb+srv://ciceraribeiro:yTwSUXAxsJqu80Qu@planify.48zvn.mongodb.net/?retryWrites=true&w=majority
    volumes:
      - .:/usr/src/app
